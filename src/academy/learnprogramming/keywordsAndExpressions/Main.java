package academy.learnprogramming.keywordsAndExpressions;

public class Main {

    public static void main(String[] args) {
	    // What are expressions?
        // Expressions are essential building blocks of Java.
        // Expressions are build with values, variables, operators and method calls
        // Creating an expressions to convert miles to kilometres (1 mile = 1.609344 kilometres):
        // Everything is an expressions in the line below except 'double' data type.
        double kilometres = (100 * 1.609344);

        // 'highScore = 50' is a expression
        int highScore = 50;

        // If statement below 'highScore == 50' is the expression
        if (highScore == 50) {
            System.out.println("This is an expression");
        }
    }
}
